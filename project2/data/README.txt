Place CUAD SQuAD bundle here:

- `CUAD/CUADv1.json`

Optional small synthetic clauses (JSONL, same schema as corpus lines):

- `synthetic_clauses.jsonl`

After running `python project2/ingestion/chunker.py` (with `PYTHONPATH=.`), the merged retrieval file is:

- `corpus.jsonl`
